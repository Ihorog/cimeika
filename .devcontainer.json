{
  "tasks": {
    "test": "./gradlew test",
    "build": "sudo apt-get update && sudo apt-get install openjdk-21-jdk && chmod +x gradlew && ./gradlew build",
    "launch": "python3 -m http.server"
  }
}